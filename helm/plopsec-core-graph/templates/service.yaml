apiVersion: v1
kind: Service
metadata:
  name: {{ include "plopsec-core-graph.fullname" . }}
  labels:
    app: {{ include "plopsec-core-graph.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: {{ include "plopsec-core-graph.name" . }}
